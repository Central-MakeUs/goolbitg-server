<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.goolbitg.api.v1.repository.mappers.ChallengeRecordCustomMapper">

  <select id="findByUserIdAndDateBetween" resultType="ChallengeRecordCustom">
    select chr.status as status, ch.category as category from challenge_records chr
    join challenges ch on ch.id = chr.challenge_id
    where chr.date between #{startDate} and #{endDate}
    and chr.user_id = #{userId};
  </select>

</mapper>
